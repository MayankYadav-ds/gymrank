# Environment Strategy

## Local

Local development uses Dockerized PostgreSQL and `.env` files copied from committed templates.

## Backend Environment Variables

- `NODE_ENV`
- `PORT`
- `DATABASE_URL`
- `APP_NAME`
- `CORS_ORIGIN`

## Mobile Environment

The mobile skeleton currently keeps environment handling minimal. API environment selection should be finalized before feature implementation.
